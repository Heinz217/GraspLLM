# Core stack — pin to a known-good combination that matches the README setup.
# (Tested with PyTorch 2.0–2.1 / CUDA 11.8; torch is installed separately
# via the official wheel index — see README §1.)
transformers>=4.51.0,<5.4.0
accelerate>=0.30
deepspeed>=0.14,<0.18
peft>=0.10
sentencepiece>=0.1.99
huggingface_hub>=0.20

# Graph + numeric (torch_geometric and pyg_lib install via the PyG wheel index)
torch_geometric==2.6.1
networkx>=3.0
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0

# Dataset / preprocessing utilities
einops>=0.7
POT>=0.9.5
pyhocon>=0.3.61

# Eval-only deps
sentence-transformers>=2.5

# Logging / misc
wandb>=0.15
shortuuid>=1.0.11
tqdm>=4.66
